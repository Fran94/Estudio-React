

const user = {
    username: "Andres",
    email: "correo@google.com",
    age: 20,
    ranking: 9,
};

const {username, ranking, age} = user;

console.log(`${username} tiene ${age} años de edad`);
console.log(ranking);